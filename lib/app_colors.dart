import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.blue;
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;

  // Background Gradient
  static const Color bgGradientStart = Color.fromARGB(255, 207, 231, 255);
  static const Color bgGradientEnd = Color.fromARGB(255, 255, 255, 255);

  // Grid Item
  static final Color gridStart = Colors.blue.shade300;
  static final Color gridEnd = Colors.blue.shade600;
  static final Color gridShadow = Colors.blue.withOpacity(0.4);
}
