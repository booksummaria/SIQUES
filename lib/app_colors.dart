import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.blue;
  static const Color secondary = Colors.orange;
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;

  // Background Gradient
  static const Color bgGradientStart = Color.fromARGB(255, 255, 224, 178);
  static const Color bgGradientEnd = Color.fromARGB(255, 255, 255, 255);

  // Grid Item
  static final Color gridStart = Colors.orange.shade300;
  static final Color gridEnd = Colors.orange.shade600;
  static final Color gridShadow = Colors.orange.withOpacity(0.4);
}
